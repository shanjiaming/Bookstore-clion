//
// Created by JiamingShan on 2021/1/28.
//

#include "BPlusTree.h"



#ifdef stub
/*template<class T>
T BPlusTree<T>::find(const StringType &key){
    auto ret = Map.find(key);
    if (ret == Map.end()) throw Not_Found();
    return *ret.second;
}

template<class T>
void BPlusTree<T>::insert(const T &o){
    Map[o.key] = o;
}

template<class T>
void BPlusTree<T>::erase(const StringType &key){
    Map.erase(key);
}*/
#endif